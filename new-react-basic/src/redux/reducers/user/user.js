const init_state = {
  id: 0,
  username: "ini redux",
};

function userReducer(state = init_state, action) {
  //kumpulan action
  //akan mempengaruhi state
  return state;
}

export default userReducer;
